# Explore With Me - Этап 2: Микросервисы

## 📋 Описание проекта
Это вторая часть дипломного проекта "Explore With Me" - сервис для поиска событий и участия в них.
На этом этапе мы преобразуем монолитный сервис в набор микросервисов.

## 🏗️ Архитектура

### Компоненты системы:
1. **API Gateway** (порт 8080) - единая точка входа
2. **Discovery Server** (Eureka, порт 8761) - сервис обнаружения
3. **Config Server** (порт 8888) - централизованная конфигурация
4. **Main Service** (порт 8081) - агрегатор запросов
5. **Микросервисы** (порты 8082+) - отдельные сервисы

### Схема взаимодействия:
Клиент → API Gateway → Main Service → [Микросервисы]
↗
Eureka ← Config Server


### Выделенные микросервисы:
1. **User Service** (8082) - управление пользователями
2. **Category Service** (8083) - управление категориями
3. **Event Service** (8084) - управление мероприятиями
4. **Request Service** (8085) - управление заявками
5. **Comment Service** (8086) - управление комментариями
6. **Compilation Service** (8087) - управление подборками

## 🛠 Технологии
- **Java 21**
- **Spring Boot 3.3.4**
- **Spring Cloud 2023.0.3**
- **PostgreSQL** - базы данных
- **Docker & Docker Compose** - контейнеризация
- **Eureka** - service discovery
- **OpenFeign** - межсервисная коммуникация
- **Resilience4j** - устойчивость к сбоям

## 🚀 Запуск проекта

### 1. Требования:
- Docker и Docker Compose
- Maven 3.8+
- Java 21

### 2. Сборка:
```
# Собрать все модули
mvn clean install

# Или собрать отдельные сервисы
mvn clean install -pl core/main-service -am
mvn clean install -pl core/user-service -am
```

### 3. Запуск в Docker:
```
# Запустить все сервисы
docker-compose up -d --build

# Проверить логи
docker-compose logs -f

# Остановить
docker-compose down
```

### 4. Проверка работы:

Eureka Dashboard: http://localhost:8761

API Gateway: http://localhost:8080

Основное API: http://localhost:8080/events, /users, /categories и т.д.

## Базы данных:

Каждый микросервис имеет свою БД:

ewm-users - User Service
ewm-categories - Category Service
ewm-events - Event Service
ewm-requests - Request Service
ewm-comments - Comment Service
ewm-compilations - Compilation Service

